[package]
name = "rs-web"
version = "0.1.0"
edition = "2024"
description = "A fast, Lua-driven static site generator with encryption support"
authors = ["Ray Andrew Sinurat"]
repository = "https://github.com/rslib/web"
license = "MIT"
keywords = ["static-site", "blog", "markdown", "encryption", "lua"]
categories = ["command-line-utilities", "web-programming"]
include = ["/src/**/*", "/Cargo.toml", "/Cargo.lock", "/LICENSE", "/README.md"]

[dependencies]
# Markdown
pulldown-cmark = { version = "0.13", features = ["simd"] }

# Templates (runtime)
tera = { version = "1.20", features = ["date-locale"] }

# Config & serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.9"

# Lua scripting
mlua = { version = "0.11", features = [
  "lua54",
  "vendored",
  "serialize",
  "send",
] }

# Date handling
chrono = { version = "0.4", features = ["serde"] }

# Image processing
image = { version = "0.25", default-features = false, features = [
  "jpeg",
  "png",
] }
webp = "0.3"

# CSS minification
minifier = "0.3"

# File operations
walkdir = "2.5"
ignore = "0.4"
glob = "0.3"
dirs = "6.0"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Parallel processing
rayon = "1.11"

# Concurrent data structures & caching
dashmap = "6.1"
parking_lot = "0.12"
twox-hash = "1.6"
postcard = { version = "1.1", features = ["alloc"] }

# CLI
clap = { version = "4.5", features = ["derive"] }

# Logging
log = "0.4"
env_logger = "0.11"

# Encryption
aes-gcm = "0.10"
argon2 = "0.5"
base64 = "0.22"
rand = "0.8"

# Regex for encrypted blocks
regex = "1.12"
once_cell = "1.21"

# File watching
notify = "8.2"
notify-debouncer-mini = "0.7"

# Git metadata
git2 = { version = "0.20", default-features = false }

# URL encoding
urlencoding = "2.1"

# Async runtime & HTTP client
tokio = { version = "1.49", features = ["full"] }
reqwest = { version = "0.13", features = ["json"] }
futures = "0.3"

# Dev server
axum = { version = "0.8", features = ["ws"] }
tower-http = { version = "0.6", features = ["fs", "cors"] }

[dev-dependencies]
tempfile = "3.24"

[profile.release]
lto = true
codegen-units = 1
strip = true
